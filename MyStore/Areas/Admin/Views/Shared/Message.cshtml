@if (TempData.ContainsKey("message"))
{
    MyStore.Libarary.XMessage message = (MyStore.Libarary.XMessage)TempData["message"];
    <div class="alert alert-@message.TypeMsg">
        @message.Msg
    </div>
}